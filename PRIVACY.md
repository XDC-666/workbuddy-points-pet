# 隐私说明（Privacy）

本桌宠是 **本地优先（local-first）** 的小工具，设计上不收集、不上传你的任何数据。

## 本工具不会做的事

- ❌ 不上传你的 WorkBuddy 账号、积分、提示词、对话内容或文件。
- ❌ 不连接任何远程服务器（除你自己配置的「HTTP 余额数据源」外）。
- ❌ 不内置遥测 / 埋点 / 统计 SDK。
- ❌ 不开放任何外部可访问的网络端口（无本地 HTTP 服务）。
- ❌ 不请求屏幕、麦克风、摄像头、剪贴板等系统权限。

## 数据存哪儿

- 你的积分、API Key（如需）、语录等配置只存在**本机用户目录**的 `config.json` 中
  （Windows 通常在 `%APPDATA%/workbuddy-points-pet/config.json`）。
- 该文件 **不会被本仓库提交或上传**（已在 `.gitignore` 中忽略）。

## HTTP 数据源模式说明（使用须知）

如果你在设置里选择「HTTP 余额接口」模式：

- 桌宠会**按你填的 URL 主动发起请求**去读取余额。
- 请仅填写你**自己信任**的接口（如官方余额 API 或自建服务）。
- 请求会携带你在设置里填写的 `Authorization` 头；不要把私人令牌填进不可信的 URL。
- 该请求从你的电脑发出，理论上对方接口可看到请求来源 IP 与时间——这是你主动发起的，非后台偷传。

## WorkBuddy 状态钩子（可选功能）

如果你运行了 `npm run install-hook`：

- 钩子脚本（`scripts/status-hook.mjs`）**只写入结构性字段**：事件名、时间戳、会话 ID、工具名、通知类型。
- **不会**写入提示词、回答内容、对话记录、文件内容、工具参数或工具结果。
- spool 文件保存在本机 `~/.workbuddy-points-pet/events.spool`，不上传、不联网，超过 2 MB 自动轮转。
- 自动积分上报会在同目录生成 `balance.json`（剩余积分估算值）与 `prefs.json`（初始积分 / 消耗配置），**均仅存于本机、不上传、不联网**。余额是基于交互次数估算的消耗值，并非真实账单数字。
- 安装脚本只在本机改写 `~/.workbuddy/settings.json`，**不读取、不打印、不上传**其中的任何密钥，且每次都会生成带时间戳的备份。

## 安全建议

1. 只从本仓库或你信任的 Release 下载运行，慎用来路不明的 fork 版本。
2. 打包后的 `.exe` 可能被 Windows Defender 误报，属 Electron 应用通病；如需消除误报可对安装包做代码签名。
3. 若未来自行加入本地 HTTP 服务，务必绑定 `127.0.0.1`（仅本机），不要绑定 `0.0.0.0`。

## 商标

WorkBuddy 是其各自所有者的产品名称。本项目是独立的社区开源工具，**与 WorkBuddy 无隶属关系，也未获其背书**。内置角色形象为原创示例素材，可替换。
